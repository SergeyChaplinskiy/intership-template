<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>Node js Import CSV File to mongoDB Example</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
  </head>
  <body>
    <form action="/v1/books/download" method="post" enctype="multipart/form-data">
      <div class="mb-3">
        <input type="file" class="form-control" name="csvFile" />
      </div>
      <button type="submit" class="btn btn-success">Download</button>
      <%if(data){%>
      <div class="container mt-4">
        <table class="table">
          <thead>
            <tr>
              <!-- <th>#Id</th> -->
              <th>code3</th>
              <th>title</th>
              <th>description</th>
              <!-- <th>Mobile</th> -->
            </tr>
          </thead>
          <tbody>
            <%for(var i=0; i < data.length; i++){%>
            <tr>
              <td><%= i+1%></td>
              <td><%= data[i].code3%></td>
              <td><%= data[i].title%></td>
              <td><%= data[i].description%></td>
              <!-- <td><%= data[i].Mobile%></td> -->
            </tr>
            <%}%>
          </tbody>
        </table>
      </div>
      <%}%>
    </form>
  </body>
</html>